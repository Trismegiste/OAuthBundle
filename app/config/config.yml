imports:
    - { resource: security.yml }

framework:
    #esi:             ~
    #translator:      { fallback: "%locale%" }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_proxies: ~
    session:         ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            %kernel.debug%
    strict_variables: %kernel.debug%
    form:
        resources:
            - 'TrismegisteFrontBundle:theme:form_errors.html.twig'
            - 'TrismegisteFrontBundle:theme:form_item.html.twig'

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [ ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: "%kernel.root_dir%/Resources/java/compiler.jar"
        #yui_css:
        #    jar: "%kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar"

dokudoki:
    stage: blackmagic
    server: %mongodb_server%
    database: Sf2ffbp
    collection: boilerplate

hwi_oauth:
    # name of the firewall in which this bundle is active, this setting MUST be set
    firewall_name: secured_area
    resource_owners:
        my_github:
            type:                github
            client_id:           51a83ff9f1216abd83ee
            client_secret:       36a8f497751ba31321ad47fbba68fc42eb24bf8e
            scope: ""
    http_client:
        verify_peer:   true
        ignore_errors: false
    target_path_parameter: /admin/dashboard